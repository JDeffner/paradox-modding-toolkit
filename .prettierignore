node_modules/
dist/
packages/*/dist/
packages/vscode/data/
pnpm-lock.yaml
# Harvested/generated data: byte-for-byte reproducible from scripts/, never hand-edited.
packages/server/data/
# Bundled wiki mirrors (CC BY-SA) and the skill prose keep their upstream shape.
packages/vscode/skills/
# Test fixtures are exact game-file samples; reformatting changes what is under test.
packages/server/test/fixtures/
# Calibration probe mods are real game files (BOM'd loc yml, paradox spacing);
# a formatter would corrupt what the game loads.
docs/gui-designer/
# Prose is written to the conventions in AGENTS.md (no em dashes, hand-wrapped
# tables, deliberate line breaks); a formatter has no useful opinion about it.
*.md

