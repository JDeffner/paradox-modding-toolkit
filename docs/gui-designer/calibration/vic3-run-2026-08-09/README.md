# Vic3 probe run artifacts, 2026-08-09

Screenshots of the five probe windows (game 1.13.9, 1920x1080, 100% GUI
scaling) plus the `analyze.ps1` outputs. `px_probe_a.png` is a re-encode of
a LOSSY WebP (chat transfer) — component edges carry ±1px noise there; the
ruler still resolved scale 1.0000. b-e are clean PNGs. Conclusions:
`../vic3-expectations.md`.
