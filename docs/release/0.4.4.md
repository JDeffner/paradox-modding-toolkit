# Paradox Modding Toolkit 0.4.4 (beta)

- Fix localization edits, settings reloads and vanilla cache isolation.
- Share texture backgrounds between DDS editors and hover previews; clarify game-data setup.
- Preserve webview state during development reloads, including unsaved Event Graph edits.
- Check release tags against package versions, prepare notes automatically, and announce on Discord only after Marketplace publishing succeeds.

Includes `@px-lsp/server` 0.3.5 and `@px-lsp/protocol` 0.2.5.
